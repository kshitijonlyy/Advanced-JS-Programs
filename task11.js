// create an arrow function with two parameters 'x, y'. perform the logics to print multiplication of the two numbers.

var multiply = (x, y) => {
    console.log('Product of ' + x + ' and ' + y + ' is: '+ (x*y));
}

multiply(10,4);